<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="UserMapper">
    <select id="selectList">
        SELECT * FROM user
    </select>
    <insert id="insertuser">
        insert into user 
        (lolginId, pwd, userName, bthDate, email, postNum, address, phoneNum, profile, regDate)
        values
        ('hello', 'hello1234', 'jinho', null, 'hello@naver.com', 22, 'seoul', '01012345678', 'jdskjskdskjdkjsdk', '2012-12-22');
    </insert>
</mapper>